package example

// Packages are used to organize related classes into namespaces, while modules collect all the classes in a program or library and must be compiled together
infix fun Int.add(b : Int) : Int = this + b